import {
    StyleSheet
} from 'react-native';
import { SIZES } from '../../assets/theme';

export default StyleSheet.create({
   container:{
       backgroundColor:'green',
       height:SIZES.height,
       width:SIZES.width
   }

})